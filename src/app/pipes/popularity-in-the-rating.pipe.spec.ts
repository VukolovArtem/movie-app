import { PopularityInTheRatingPipe } from './popularity-in-the-rating.pipe';

describe('PopularityInTheRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new PopularityInTheRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
