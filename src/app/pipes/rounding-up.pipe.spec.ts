import { RoundingUpPipe } from './rounding-up.pipe';

describe('RoundingUpPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundingUpPipe();
    expect(pipe).toBeTruthy();
  });
});
